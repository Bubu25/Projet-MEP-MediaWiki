---

- name : installation de mediawiki
  become: true
  hosts: mediawiki
  tasks:

    
    - name: directory of signature files
      file:
        path: /etc/conf_machine
        state: directory

    - name: Creating an empty file
      file:
        path: "/etc/conf_machine/signature.conf"
        state: touch
      become: yes


    - name: "edit apparmor alias"
      lineinfile:
        dest: /mnt/APPLI-SERVER/appli/projects/MWA/apache2/MWA_mediawiki/LocaLSettings.php
        regexp: '^\$wgLogos'
        line: '$wgLogos'
  
...